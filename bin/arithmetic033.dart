// Create function func with arguments
// the task condition is in the Readme file
double func(int x, int c, int v) {
  double b = 2 * (x + c) / 2 * (v / 2);
  return b;
}

void main() {
  print(func(1, 11, 4));
}
